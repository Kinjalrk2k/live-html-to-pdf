const firebaseConfig = {
  apiKey: process.env.REACT_APP_FIREBASE_API_KEY,
  authDomain: "live-html-to-pdf.firebaseapp.com",
  projectId: "live-html-to-pdf",
  storageBucket: "live-html-to-pdf.appspot.com",
};

export default firebaseConfig;
